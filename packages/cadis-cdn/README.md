# cadis-cdn

`cadis-cdn` provides reusable dataset CDN primitives for Cadis:

- URL resolution for manifest-relative paths
- HTTP JSON/text/bytes retrieval
- SHA-256 checksum helpers
- Safe `.tar.gz` extraction
